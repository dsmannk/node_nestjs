# node_nestjs

## 설치
```
$ mkdir chapter9
$ cd chapter9
$ nest new config-test // 에러가 나면 npx @nestjs/cli new config-test를 실행하세요.
$ cd config-test
$ npm i @nestjs/config // 의존성 패키지 설치 (dotenv를 사용)s 
```

